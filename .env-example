# Application Settings
APP_NAME="Devrim Tuncer"
APP_ENV=production
# APP_KEY: Laravel'in şifreleme için kullandığı anahtar. 
# Coolify'de deploy ederken otomatik oluşturulur veya "php artisan key:generate" ile oluşturulur.
# ÖNEMLİ: Bu değer boş bırakılamaz! "php artisan key:generate" komutu ile oluşturulmalıdır.
APP_KEY=base64:hzBaV2/fFtKmJm23AJakxk73V1hU3kV8qbVVKcvCLcQ=
# APP_DEBUG: Laravel'in hata gösterim modu (true/false)
# Production'da false olmalı (güvenlik için)
APP_DEBUG=false
APP_URL=https://devrimtuncer.com

# Logging Settings
# LOG_CHANNEL: Log kanalı (stack, single, daily, slack, syslog, errorlog, null)
# stack: Birden fazla kanalı birleştirir (production için önerilir)
LOG_CHANNEL=stack
# LOG_DEPRECATIONS_CHANNEL: Deprecated uyarıları için log kanalı
LOG_DEPRECATIONS_CHANNEL=null
# LOG_LEVEL: Log seviyesi (emergency, alert, critical, error, warning, notice, info, debug)
LOG_LEVEL=debug

# Database Settings (PostgreSQL)
DB_CONNECTION=pgsql
DB_HOST=a4gos04840sg8488c44s0o0k
DB_PORT=5432
DB_DATABASE=postgres
DB_USERNAME=postgres
DB_PASSWORD=eXVKd4M8r7iIXqJXvV5I34W44qWHika4SwvRrvyeulIn0J28Z9l9kMJ1Fzdk8nbB

# PostgreSQL URL (Alternative - if provided, will override above settings)
# Format: postgres://username:password@host:port/database
DATABASE_URL=postgres://postgres:eXVKd4M8r7iIXqJXvV5I34W44qWHika4SwvRrvyeulIn0J28Z9l9kMJ1Fzdk8nbB@a4gos04840sg8488c44s0o0k:5432/postgres

# DEBUG_MODE: Uygulama özel debug modu (APP_DEBUG'den farklı)
# APP_DEBUG: Laravel framework'ün hata gösterimi
# DEBUG_MODE: Uygulama içi özel debug kontrolleri için kullanılır
DEBUG_MODE=false

# Admin
ADMIN_PATH=/qTnzV62SjDYBsPaI

# Upload
MAX_UPLOAD_SIZE=10
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp

# Pagination
BLOGS_PER_PAGE=9
PORTFOLIOS_PER_PAGE=12


LOCATION_MAP_URL=https://www.google.com/maps/embed?pb=...

# Rate Limiting
MESSAGE_RATE_LIMIT=5
MESSAGE_RATE_PERIOD=3600

# Cache & Session
CACHE_DRIVER=file
FILESYSTEM_DISK=local
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Queue Settings
# QUEUE_CONNECTION: Kuyruk bağlantı türü
# sync: Senkron (anında işlenir, production için uygun)
# database: Veritabanı tablosu kullanır
# redis: Redis kullanır (performans için)
# sqs: AWS SQS kullanır
QUEUE_CONNECTION=sync

# Broadcasting (Not used in this project)
# BROADCAST_DRIVER=log

# Mail
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Vite Settings (Frontend Build Tool)
# VITE_APP_NAME: Vite build tool için uygulama adı
# Frontend asset'lerini build ederken kullanılır
VITE_APP_NAME="${APP_NAME}"

